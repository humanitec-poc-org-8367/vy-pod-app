apiVersion: score.dev/v1b33

metadata:
  name: "vy-pod-app"

servicee:
  ports:
    www:
      port: 80 # The port that the service will be exposed on
      targetPort: 9898 # The port that the container will be listening on

containeer:
  frontend:
    variables:
      HELLO: WORLD
      NAME: VIV_Y

 

resources:
  dns: # We need a DNS record to point to the service 
    type: dns
